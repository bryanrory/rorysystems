{
  "name": "rory-contact-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Endpoint de envio do formulário de contato da landing (SMTP Brevo via Cloudflare Workers).",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "worker-mailer": "^1.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
